#include "Contact.h"

void Contact::createContact(std::string fname,int CQ)
{
	/*const char* filename = fname.c_str();
	
	//std::fstream ConBase;

	std::string name, surname, number;
	char opt = '1';

	do {
		system("cls");

		std::cout << "Dane zapisywane sa w formacie: imie, nazwisko, nr telefonu" << '\n';
		std::cout << "Podaj imie: ";
		std::cin >> name;
		system("cls");

		std::cout << "Dane zapisywane sa w formacie: imie, nazwisko, nr telefonu" << '\n';
		std::cout << "Podaj nazwisko: ";
		std::cin >> surname;
		system("cls");

		std::cout << "Dane zapisywane sa w formacie: imie, nazwisko, nr telefonu" << '\n';
		std::cout << "Podaj numer telefonu: ";
		std::cin >> number;
		system("cls");

		do {
			std::cout << "Czy podane dane sie zgadzaja?" << '\n';
			std::cout << "IMIE:				" << name << '\n';
			std::cout << "NAZWISKO:			" << surname << '\n';
			std::cout << "NUMER TELEFONU:	" << number << '\n';
			std::cout << "[0] NIE		[1] TAK		[2] ANULUJ I WYJDZ" << '\n';
			opt = _getch();
		} while (opt < 48 || opt > 50);

	} while (opt < 49 || opt > 50);

	if (opt != 50) {

		std::cin >> name;
		std::cin >> surname;
		std::cin >> number;
		//std::cin >> CQ;
		CQ++;

		std::cout << "Poprawnie zapisano kontakt!" << '\n';
		Sleep(1000);

		std::string cqstr = std::to_string(CQ + 1);
		std::string Array[4] = { name, surname, number, cqstr};

			return Array;
	}*/
}
